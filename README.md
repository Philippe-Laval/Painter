# Painter

Port to AvaloniaUI 12.1.1

Shows a AvaloniaUI Custom Control that uses Skia operations for drawing on a SKBitmap.
The bitmap is then rendered on the Custom Control.

Shows how to implement a ICustomDrawOperation (SkiaBitmapCustomDrawOperation) 

Supports only line tool.
Flicker problem when drawing.

